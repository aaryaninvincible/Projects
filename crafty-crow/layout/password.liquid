<!doctype html>
<html lang="{{ request.locale.iso_code }}">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>{{ shop.name }} – {{ 'general.password_page.login_form_heading' | t }}</title>
    {{ 'theme.css' | asset_url | stylesheet_tag }}
    {{ content_for_header }}
  </head>
  <body class="cc-body cc-password">
    <main id="MainContent" role="main" class="cc-password-main">
      <div class="cc-password-card">
        <h1 class="cc-logo-wordmark">Crafty Crow</h1>
        <p class="cc-password-text">
          {{ 'general.password_page.admin_link_html' | t }}
        </p>
        {{ content_for_layout }}
      </div>
    </main>
    {{ content_for_footer }}
  </body>
</html>

